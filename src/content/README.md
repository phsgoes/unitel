# Content drafts

Markdown drafts under this folder are **source outlines**, not wired into the site runtime.

Live solution-page copy lives in `src/data/solucoes/*.ts`. Prefer editing those modules (or migrating these drafts into that SSOT) instead of duplicating text in `.astro` pages.
